#pragma once

namespace state
{

enum class id
{
    character_select,
    game_over,
    game,
    level_complete,
    // loading,
    // menu,
    // none,
    pause,
    // settings,
    title,
};

}
